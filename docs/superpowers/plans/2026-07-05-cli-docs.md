# CLI Docs Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete external documentation for the finished CLI surface: a full command reference in `docs/cli/`, and updated README + product-vision sections. (Spec: [CLI Production Readiness](../specs/2026-07-04-cli-production-readiness-design.md), plan 6.)

**Architecture:** Text only. Reference content is derived from the actual `--help` output and the plan-5 audit table — never written from memory.

**Tech Stack:** Markdown.

## Global Constraints

- **Prerequisite:** CLI plans 1-5 merged. If plan 5's consistency pass hasn't landed, stop (the reference would document a surface about to change).
- Feature branch `feature/cli-docs`, PR to `main`.
- Optimize for readability. Every documented command/flag/exit code must be verified against the real binary, not assumed.
- If cleanup plan C (docs simplification) already landed, respect its README structure (end-user front door at top); if not, add the CLI reference link where the current README references `docs/cli/windows-install.md`.

## Resume instructions

`git log --oneline -10` shows landed commits; unchecked boxes remain. T1 → T2 → T3 in order.

---

### Task 1: Command reference

**Files:**
- Create: `docs/cli/command-reference.md`
- Inspect first: run the built CLI (`dotnet run --project src/Tools/AHKFlowApp.CLI -- --help` and each `<group> --help`, `<group> <verb> --help`) and capture real output; plan-5 audit table for exit codes and conventions

- [ ] **Step 1:** Write the reference:
  - Header: what `ahkflow` is, install pointer (`winget install AHKFlow.CLI` → windows-install.md), configuration (`ApiBaseUrl` via appsettings/`AHKFLOW_*` env), auth model (device-code `login`/`logout`).
  - Global conventions section: data → stdout, diagnostics → stderr, `--json` on every data command, `--verbose`, exit codes table (0 success / 1 config-server-transport / 2 user error / 3 auth), id-or-name addressing rules per vertical (profile/category by name, hotstring by trigger, hotkey by id).
  - Per group (`login`, `logout`, `download`, `hotstring`, `hotkey`, `category`, `profile`): one table per verb — flags, aliases, required/default, one realistic example with real captured output (trimmed).
  - Scripting recipes: 3-4 real pipelines (e.g. `ahkflow hotstring list --json | jq '.items[].trigger'`; create-many loop; profile-scoped zip download).
- [ ] **Step 2:** Spot-run EVERY example verbatim against a local API; fix drift.
- [ ] **Step 3: Commit** `docs(cli): full command reference`

### Task 2: Link + reconcile existing CLI docs

**Files:**
- Modify: `docs/cli/windows-install.md` — add a "Next steps" link to the command reference; verify install instructions still match the built binary
- Inspect: `docs/cli/winget-submission.md` — historical release runbook, leave content, add nothing (no release planned; roadmap decision #2)
- Modify: `AGENTS.md` — if its CLI mentions enumerate commands anywhere, align; otherwise no change

- [ ] **Step 1:** Apply; check inbound links (`grep -rn "command-reference\|windows-install" README.md AGENTS.md docs --include="*.md"`).
- [ ] **Step 2: Commit** `docs(cli): wire reference into existing docs`

### Task 3: README + product vision

**Files:**
- Modify: `README.md` — CLI paragraph: one-line description, the command surface (top-level `login`/`logout` plus the `download`, `hotstring`, `hotkey`, `category`, `profile` groups), link to the reference
- Modify: `docs/architecture/product-vision.md` §8.3 "Current CLI areas" — replace the six-command list with the full surface (grouped: auth, hotstring, hotkey, category, profile, download); §6 Future list: remove "CLI commands for managing hotkeys" (now shipped)

- [ ] **Step 1:** Apply both; re-read §5/§6 for any other now-stale CLI statement.
- [ ] **Step 2:** Link check the touched files (every `](path)` target exists).
- [ ] **Step 3: Commit** `docs: README + product vision reflect full CLI surface`

---

## Final verification

- [ ] Every example in `command-reference.md` was executed verbatim and matches its shown output
- [ ] `grep -n "hotkey" docs/architecture/product-vision.md` — no "pending CLI hotkey" statements remain
- [ ] Zero broken relative links in touched files
- [ ] PR: single concern, docs only
